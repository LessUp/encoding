# OpenSpec Configuration for Encoding Project
profile: core
delivery: both  # Generate both skills and commands for Claude Code AND Cursor

workflows:
  - propose
  - apply
  - archive
  - sync-specs

featureFlags:
  bilingual-docs: true           # Chinese/English support
  cross-language-verification: true  # Project-specific requirement

# Project metadata
project:
  name: encoding
  description: Multi-language compression algorithm library
  languages: [cpp, go, rust]
