# 2025-11-24 Rust 哈夫曼编码实现

- 新增 `rust/main.rs`
- 实现静态模型哈夫曼编码压缩与解压：
  - 统一 0-255 字节 + EOF 共 257 个符号
  - 使用频率表构建哈夫曼树，生成前缀码
  - 压缩：`encode input output`（写入魔数 `HFMN` + 频率表 + 编码比特流）
  - 解压：`decode input output`（读取头部频率表并重建哈夫曼树后解码）
- 命令行接口与 C++/Go 版本保持一致，便于跨语言对比与测试。
