[package]
name = "huffman"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"

[lib]
name = "huffman"
path = "src/lib.rs"

[dependencies]
compresskit-codec = { path = "../../shared/rust" }

[dev-dependencies]
