{
  "name": "compress-kit",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "cd docs && npm run dev",
    "docs:build": "cd docs && npm run build",
    "docs:preview": "cd docs && npm run preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
